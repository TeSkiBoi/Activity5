import { encryptWithPassword } from "../crypto/cryptography.js"

function insertUser() {
    userID,
        loginID,
        hashedpassword,
        salt,
        iv
}

function insertStudent() {
    studentID,
        username,
        contact,
        year,
        course,
        userID
}